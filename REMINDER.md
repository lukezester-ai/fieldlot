# Fieldlot — статус

## Готово
- Лендинг + dev API + Vercel `api/` handlers
- Български грешки, anti-bot, футър с контакт
- `.gitignore` включва `.local/`

## Остава (ръчно)
- Commit + `git push` към GitHub
- Vercel project + env vars (Resend, inbox)
- По желание: LLM ключ за чат
